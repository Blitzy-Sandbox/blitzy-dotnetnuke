// Per-folder barrel for the Module feature data-access services.
// Re-exports ModuleService so consumers import from `../services` rather than the concrete file.
export { ModuleService } from './module.service';
