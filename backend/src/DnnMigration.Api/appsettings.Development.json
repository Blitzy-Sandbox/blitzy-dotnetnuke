{
  "ConnectionStrings": {
    "Default": "Server=(localdb)\\MSSQLLocalDB;Database=DnnMigration;Trusted_Connection=True;MultipleActiveResultSets=true;TrustServerCertificate=True"
  },
  "Jwt": {
    "_comment": "DEVELOPMENT-ONLY, non-secret HS256 signing key (>= 256 bits). NEVER used outside the Development environment; Production/Staging must supply a strong 'Jwt:SecretKey' via environment variable or user-secrets (Program.cs fails fast otherwise). See MIGRATION_NOTES.md.",
    "SecretKey": "dnn-migration-development-only-jwt-signing-key-not-for-production-use"
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft.AspNetCore": "Information"
      }
    }
  },
  "DetailedErrors": true
}
